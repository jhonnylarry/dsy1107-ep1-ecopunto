# backend/ — pendiente

Spring Boot Resource Server que validará los Access Token emitidos por Microsoft Entra ID (issuer, audience, firma, expiración) y aplicará autorización por scope/rol sobre las entidades `PuntoLimpio` y `Reporte`.

Se construye siguiendo el starter oficial `spring-resource-server` del curso.
